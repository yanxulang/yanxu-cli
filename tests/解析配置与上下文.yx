引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

引「标准:正则」为 正则；

法 须报（操作：法，代码：文）：空 则
    令 已报：理 为 假；
    令 详情：典 为 {}；
    试 则
        操作（）；
    救 所误 则
        置 已报 为 真；
        置 详情 为 言令.错误详情（所误）；
    终
    测试.断言（已报，（代码 加 「应被抛出」））；
    测试.相等（详情【「代码」】，代码）；
    归 空；
终

定 默认甲：典 为 言令.默认解析配置（）；

置 默认甲【「帮助短路」】 为 假；

定 默认乙：典 为 言令.默认解析配置（）；

测试.断言（默认乙【「帮助短路」】，「默认配置调用间应隔离」）；

定 合并配置：典 为 言令.解析配置（{「允许穿插选项」：假，「拒绝重复开关」：真}）；

测试.断言（非 合并配置【「允许穿插选项」】，「覆盖值应生效」）；

测试.断言（合并配置【「拒绝重复开关」】，「第二个覆盖值应生效」）；

测试.断言（合并配置【「帮助短路」】，「未覆盖值应保留默认」）；

法 未知配置（）：空 则
    言令.解析配置（{「不存在」：真}）；
    归 空；
终

法 配置类型错误（）：空 则
    言令.解析配置（{「帮助短路」：「真」}）；
    归 空；
终

须报（未知配置，「YANLING_CONFIG_KEY」）；

须报（配置类型错误，「YANLING_CONFIG_VALUE」）；

定 穿插命令 为 言令.命令（「穿插」，「」）；

穿插命令.加开关（「verbose」，「v」，「」）.加余项（「各项」，「」）；

定 穿插结果：典 为 穿插命令.解析（【「文件」，「--verbose」】）；

测试.断言（穿插结果【「选项」】【「verbose」】，「默认应允许位置参数后继续解析选项」）；

测试.相等（穿插结果【「参数」】【「各项」】【0】，「文件」）；

定 停止结果：典 为 穿插命令.按配置解析（【「文件」，「--verbose」】，{「允许穿插选项」：假}）；

测试.断言（非 停止结果【「选项」】【「verbose」】，「禁用穿插后开关应保持默认」）；

测试.相等（停止结果【「参数」】【「各项」】【1】，「--verbose」）；

定 帮助结果：典 为 穿插命令.按配置解析（【「--help」】，{}）；

测试.断言（帮助结果【「帮助」】，「默认配置应短路帮助」）；

定 不短路：典 为 穿插命令.尝试按配置解析（【「--help」】，{「帮助短路」：假}）；

测试.断言（非 不短路【「成功」】，「关闭帮助短路后内建帮助应按未知选项处理」）；

测试.相等（不短路【「错误详情」】【「代码」】，「YANLING_PARSE_UNKNOWN」）；

定 否定命令 为 言令.命令（「否定」，「」）；

否定命令.加开关（「color」，「c」，「」）；

测试.断言（非 否定命令.解析（【「--no-color」】）【「选项」】【「color」】，「默认应支持开关否定」）；

定 禁止否定：典 为 否定命令.尝试按配置解析（【「--no-color」】，{「允许开关否定」：假}）；

测试.断言（非 禁止否定【「成功」】，「关闭开关否定后应拒绝合成 no- 选项」）；

测试.相等（禁止否定【「错误详情」】【「代码」】，「YANLING_PARSE_UNKNOWN」）；

测试.断言（否定命令.解析（【「--color」，「--color」】）【「选项」】【「color」】，「默认允许幂等重复开关」）；

定 重复失败：典 为 否定命令.尝试按配置解析（【「--color」，「--no-color」】，{「拒绝重复开关」：真}）；

测试.断言（非 重复失败【「成功」】，「严格配置应拒绝同一开关重复」）；

测试.相等（重复失败【「错误详情」】【「代码」】，「YANLING_PARSE_DUPLICATE」）；

定 根 为 言令.命令（「root」，「根命令」）；

定 子 为 言令.命令（「child」，「子命令」）；

定 叶 为 言令.命令（「leaf」，「叶命令」）；

子.加子命令（叶）；

根.加子命令（子）；

定 路径结果：典 为 根.解析（【「child」，「leaf」，「--help」】）；

测试.相等（路径结果【「模式版本」】，言令.结果模式版本）；

测试.相等（路径结果【「命令路径」】【0】，「root」）；

测试.相等（路径结果【「子结果」】【「命令路径」】【1】，「child」）；

测试.相等（路径结果【「子结果」】【「子结果」】【「命令路径」】【2】，「leaf」）；

测试.断言（文字.始于（路径结果【「子结果」】【「子结果」】【「帮助文」】，「用法：root child leaf」），「嵌套帮助应显示完整命令路径」）；

定 建议命令 为 言令.命令（「工具」，「」）；

建议命令.加开关（「verbose」，「v」，「」）.加选项（「version」，「V」，「版本」，「文」，「」，假）.加子命令（言令.命令（「build」，「」））；

定 长建议：列<文> 为 建议命令.建议（「--ver」）；

测试.相等（长建议【0】，「--verbose」）；

测试.相等（长建议【1】，「--version」）；

测试.相等（建议命令.建议（「bu」）【0】，「build」）；

测试.相等（建议命令.建议（「-v」）【0】，「-v」）；

定 拼错：典 为 建议命令.尝试解析（【「--verbos」】）；

测试.相等（拼错【「错误详情」】【「代码」】，「YANLING_PARSE_UNKNOWN」）；

测试.断言（正则.匹配（「--verbose」，拼错【「错误详情」】【「消息」】），「单字缺失应提供长项建议」）；

定 换位：典 为 建议命令.尝试解析（【「--vrebose」】）；

测试.断言（正则.匹配（「--verbose」，换位【「错误详情」】【「消息」】），「相邻换位应提供长项建议」）；

定 无建议：典 为 建议命令.尝试按配置解析（【「--verbos」】，{「未知选项建议」：假}）；

测试.断言（非 正则.匹配（「是否想用」，无建议【「错误详情」】【「消息」】），「关闭建议后错误不应附加候选」）；

定 成功尝试：典 为 建议命令.尝试解析（【「--verbose」】）；

逐 键：文 于 【「模式版本」，「成功」，「结果」，「错误」，「错误详情」，「帮助文」】 则
    测试.断言（包含（键列（成功尝试），键），（「成功尝试缺少固定键：」 加 键））；
    测试.断言（包含（键列（无建议），键），（「失败尝试缺少固定键：」 加 键））；
终

测试.相等（成功尝试【「帮助文」】，「」）；
