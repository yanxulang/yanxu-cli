引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

引「标准:JSON」为 JSON；

令 长说明：文 为 「甲」；

当 （长度（长说明） 小于 言令.最大说明字数） 则
    置 长说明 为 （长说明 加 长说明）；
终

定 根 为 言令.命令（「根」，「」）；

令 序号：数 为 0；

当 （序号 小于 言令.最大子命令数） 则
    根.加子命令（言令.命令（（「child」 加 JSON.序列化（序号）），长说明））；
    置 序号 为 （序号 加 1）；
终

令 已报：理 为 假；

试 则
    根.帮助（）；
救 所误 则
    置 已报 为 真；
    测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_HELP_LIMIT」）；
终

测试.断言（已报，「超过一兆字的帮助文本应被拒绝」）；
