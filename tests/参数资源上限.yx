引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

引「标准:JSON」为 JSON；

定 命令 为 言令.命令（「参数」，「」）；

令 序号：数 为 0；

当 （序号 小于 言令.最大参数数） 则
    命令.加参数（（「arg」 加 JSON.序列化（序号）），「」，假）；
    置 序号 为 （序号 加 1）；
终

测试.相等（长度（命令.规格（）【「参数」】），言令.最大参数数）；

令 已报：理 为 假；

试 则
    命令.加参数（「overflow」，「」，假）；
救 所误 则
    置 已报 为 真；
    测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_SPEC_LIMIT」）；
终

测试.断言（已报，「第 257 个位置参数应被拒绝」）；
