引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

令 过长实参：文 为 「甲」；

当 （长度（过长实参） 不大于 言令.最大单实参字数） 则
    置 过长实参 为 （过长实参 加 过长实参）；
终

令 单项已报：理 为 假；

试 则
    言令.命令（「实参」，「」）.解析（【过长实参】）；
救 所误 则
    置 单项已报 为 真；
    测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_ARG_LIMIT」）；
终

测试.断言（单项已报，「超长单实参应被拒绝」）；

令 封顶实参：文 为 「甲」；

当 （长度（封顶实参） 小于 言令.最大单实参字数） 则
    置 封顶实参 为 （封顶实参 加 封顶实参）；
终

令 各实参：列<文> 为 【】；

令 总数：数 为 0；

当 （总数 不大于 （言令.最大实参总字数 除 言令.最大单实参字数）） 则
    追加（各实参，封顶实参）；
    置 总数 为 （总数 加 1）；
终

令 总量已报：理 为 假；

试 则
    言令.命令（「实参」，「」）.解析（各实参）；
救 所误 则
    置 总量已报 为 真；
    测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_ARG_LIMIT」）；
终

测试.断言（总量已报，「超过一兆字的实参总量应被拒绝」）；
