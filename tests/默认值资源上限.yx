引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

法 须报（操作：法）：空 则
    令 已报：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 已报 为 真；
        测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_SPEC_DEFAULT」）；
    终
    测试.断言（已报，「越界默认值应被拒绝」）；
    归 空；
终

法 深度过大（）：空 则
    令 默认值：任意 为 【】；
    令 深度：数 为 0；
    当 （深度 不大于 言令.最大默认嵌套深度） 则
        置 默认值 为 【默认值】；
        置 深度 为 （深度 加 1）；
    终
    言令.命令（「深度」，「」）.加默认选项（「config」，「c」，「配置」，「JSON」，「」，默认值）；
    归 空；
终

法 容器过大（）：空 则
    令 默认值：列 为 【】；
    令 数量：数 为 0；
    当 （数量 不大于 言令.最大默认容器项） 则
        追加（默认值，数量）；
        置 数量 为 （数量 加 1）；
    终
    言令.命令（「容器」，「」）.加默认选项（「config」，「c」，「配置」，「JSON」，「」，默认值）；
    归 空；
终

法 节点过多（）：空 则
    令 默认值：列 为 【】；
    令 数量：数 为 0；
    当 （数量 小于 言令.最大默认节点数） 则
        追加（默认值，【】）；
        置 数量 为 （数量 加 1）；
    终
    言令.命令（「节点」，「」）.加默认选项（「config」，「c」，「配置」，「JSON」，「」，默认值）；
    归 空；
终

须报（深度过大）；

须报（容器过大）；

须报（节点过多）；
