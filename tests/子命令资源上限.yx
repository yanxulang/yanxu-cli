引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

引「标准:JSON」为 JSON；

定 宽根 为 言令.命令（「宽根」，「」）；

令 序号：数 为 0；

当 （序号 小于 言令.最大子命令数） 则
    宽根.加子命令（言令.命令（（「child」 加 JSON.序列化（序号）），「」））；
    置 序号 为 （序号 加 1）；
终

测试.相等（长度（宽根.规格（）【「子命令」】），言令.最大子命令数）；

令 宽度已报：理 为 假；

试 则
    宽根.加子命令（言令.命令（「overflow」，「」））；
救 所误 则
    置 宽度已报 为 真；
    测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_SPEC_LIMIT」）；
终

测试.断言（宽度已报，「第 65 个直接子命令应被拒绝」）；

定 深根 为 言令.命令（「深根」，「」）；

令 当前 为 深根；

令 深度：数 为 0；

当 （深度 不大于 言令.最大子命令深度） 则
    定 子项 为 言令.命令（（「level」 加 JSON.序列化（深度）），「」）；
    当前.加子命令（子项）；
    置 当前 为 子项；
    置 深度 为 （深度 加 1）；
终

令 深度已报：理 为 假；

试 则
    深根.规格（）；
救 所误 则
    置 深度已报 为 真；
    测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_SPEC_DEPTH」）；
终

测试.断言（深度已报，「超过 32 层的子命令树应被拒绝」）；
