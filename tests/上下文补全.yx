引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

定 根 为 言令.命令（「工具」，「项目工具」）；

根.加开关（「verbose」，「v」，「详细日志」）.加选项（「config」，「c」，「环境」，「文」，「配置环境」，假）.加参数（「输入」，「可选输入」，假）；

定 构建 为 言令.命令（「build」，「构建项目」）；

构建.加开关（「release」，「r」，「发布构建」）.加选项（「output」，「o」，「路径」，「文」，「输出目录」，假）.加多值选项（「tag」，「t」，「标签」，「文」，「构建标签」）；

根.加子命令（构建）；

定 初始：典 为 根.补全（【】）；

测试.相等（初始【「模式版本」】，言令.结果模式版本）；

测试.相等（初始【「上下文」】，「候选」）；

测试.相等（初始【「命令路径」】【0】，「工具」）；

测试.断言（包含（初始【「候选」】，「--verbose」），「初始候选应包含长开关」）；

测试.断言（包含（初始【「候选」】，「--no-verbose」），「初始候选应包含开关否定形式」）；

测试.断言（包含（初始【「候选」】，「build」），「初始候选应包含子命令」）；

测试.断言（包含（初始【「候选」】，「--帮助」），「初始候选应包含中文帮助别名」）；

定 子前缀：典 为 根.补全（【「bu」】）；

测试.相等（长度（子前缀【「候选」】），1）；

测试.相等（子前缀【「候选」】【0】，「build」）；

定 嵌套：典 为 根.补全（【「build」，「--r」】）；

测试.相等（嵌套【「命令路径」】【1】，「build」）；

测试.相等（嵌套【「候选」】【0】，「--release」）；

定 长值：典 为 根.补全（【「build」，「--output」，「di」】）；

测试.相等（长值【「上下文」】，「选项值」）；

测试.相等（长值【「前缀」】，「di」）；

测试.相等（长值【「等待选项」】，「output」）；

测试.相等（长值【「值名」】，「路径」）；

测试.相等（长值【「值类型」】，「文」）；

测试.相等（长度（长值【「候选」】），0）；

定 短值：典 为 根.补全（【「build」，「-ro」，「」】）；

测试.相等（短值【「上下文」】，「选项值」）；

测试.相等（短值【「等待选项」】，「output」）；

定 附值：典 为 根.补全（【「build」，「--output=dist」，「」】）；

测试.相等（附值【「上下文」】，「候选」）；

测试.断言（非 包含（附值【「候选」】，「--output」），「不可重复的取值选项用过后不应再次建议」）；

测试.断言（包含（附值【「候选」】，「--tag」），「未使用的多值选项应继续建议」）；

定 跨级：典 为 根.补全（【「--config」，「prod」，「build」，「--r」】）；

测试.相等（跨级【「命令路径」】【1】，「build」）；

测试.相等（跨级【「候选」】【0】，「--release」）；

定 已有位置：典 为 根.补全（【「input」，「」】）；

测试.断言（非 包含（已有位置【「候选」】，「build」），「位置参数出现后不应再建议子命令」）；

测试.断言（包含（已有位置【「候选」】，「--verbose」），「默认穿插模式仍应建议选项」）；

定 禁止穿插：典 为 根.按配置补全（【「input」，「」】，{「允许穿插选项」：假}）；

测试.相等（禁止穿插【「上下文」】，「位置参数」）；

测试.相等（长度（禁止穿插【「候选」】），0）；

定 当前双线：典 为 根.补全（【「--」】）；

测试.相等（当前双线【「上下文」】，「候选」）；

测试.断言（包含（当前双线【「候选」】，「--verbose」），「尚未结束的双连字符应作为补全前缀」）；

定 已结束：典 为 根.补全（【「--」，「」】）；

测试.相等（已结束【「上下文」】，「位置参数」）；

定 帮助：典 为 根.补全（【「build」，「--help」，「」】）；

测试.相等（帮助【「命令路径」】【1】，「build」）；

测试.相等（帮助【「上下文」】，「帮助」）；

定 无效：典 为 根.补全（【「--missing」，「」】）；

测试.相等（无效【「上下文」】，「无效」）；

测试.相等（无效【「无效项」】，「--missing」）；

定 多余位置：典 为 根.补全（【「input」，「extra」，「」】）；

测试.相等（多余位置【「上下文」】，「无效」）；

测试.相等（多余位置【「无效项」】，「extra」）；

定 精简：典 为 根.按配置补全（【】，{「帮助短路」：假，「允许开关否定」：假}）；

测试.断言（包含（精简【「候选」】，「--verbose」），「精简配置仍应保留真实开关」）；

测试.断言（非 包含（精简【「候选」】，「--no-verbose」），「禁用否定时不应建议合成形式」）；

测试.断言（非 包含（精简【「候选」】，「--help」），「禁用帮助短路时不应建议内建帮助」）；

定 严格：典 为 根.按配置补全（【「--verbose」，「」】，{「拒绝重复开关」：真}）；

测试.断言（非 包含（严格【「候选」】，「--verbose」），「严格模式不应再次建议已见开关」）；

测试.断言（非 包含（严格【「候选」】，「--no-verbose」），「严格模式不应建议已见开关的否定形式」）；

测试.断言（非 包含（严格【「候选」】，「-v」），「严格模式不应建议已见开关的短名」）；

定 重复值：典 为 根.补全（【「--config」，「dev」，「--config」，「」】）；

测试.相等（重复值【「上下文」】，「无效」）；

令 长前缀：文 为 「甲」；

当 （长度（长前缀） 不大于 言令.最大名称字数） 则
    置 长前缀 为 （长前缀 加 长前缀）；
终

令 已报：理 为 假；

试 则
    根.补全（【长前缀】）；
救 所误 则
    置 已报 为 真；
    测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_SUGGEST_LIMIT」）；
终

测试.断言（已报，「补全前缀应有独立资源上限」）；
