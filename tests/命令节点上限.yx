引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

引「标准:JSON」为 JSON；

定 根 为 言令.命令（「根」，「」）；

令 组号：数 为 0；

当 （组号 小于 16） 则
    定 组 为 言令.命令（（「group」 加 JSON.序列化（组号）），「」）；
    令 项号：数 为 0；
    当 （项号 小于 言令.最大子命令数） 则
        组.加子命令（言令.命令（（（「item」 加 JSON.序列化（组号）） 加 （「_」 加 JSON.序列化（项号））），「」））；
        置 项号 为 （项号 加 1）；
    终
    根.加子命令（组）；
    置 组号 为 （组号 加 1）；
终

令 已报：理 为 假；

试 则
    根.规格（）；
救 所误 则
    置 已报 为 真；
    测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_SPEC_LIMIT」）；
终

测试.断言（已报，「超过 1024 个命令节点的规格树应被拒绝」）；
