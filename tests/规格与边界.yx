引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

法 须报（操作：法，代码：文）：空 则
    令 已报：理 为 假；
    令 详情：典 为 {}；
    试 则
        操作（）；
    救 所误 则
        置 已报 为 真；
        置 详情 为 言令.错误详情（所误）；
    终
    测试.断言（已报，（代码 加 「应被抛出」））；
    测试.相等（详情【「代码」】，代码）；
    归 空；
终

定 默认来源 为 {「嵌套」：【1】}；

定 默认命令 为 言令.命令（「默认」，「验证默认值隔离」）；

默认命令.加默认选项（「config」，「c」，「配置」，「JSON」，「结构化配置」，默认来源）；

定 首次：典 为 默认命令.解析（【】）；

置 首次【「选项」】【「config」】【「嵌套」】【0】 为 9；

定 再次：典 为 默认命令.解析（【】）；

测试.相等（再次【「选项」】【「config」】【「嵌套」】【0】，1）；

测试.相等（默认来源【「嵌套」】【0】，1）；

定 快照甲：典 为 默认命令.规格（）；

置 快照甲【「选项」】【0】【「名称」】 为 「已篡改」；

置 快照甲【「选项」】【0】【「默认」】【「嵌套」】【0】 为 7；

定 快照乙：典 为 默认命令.规格（）；

测试.相等（快照乙【「模式版本」】，言令.结果模式版本）；

测试.相等（快照乙【「选项」】【0】【「名称」】，「config」）；

测试.相等（快照乙【「选项」】【0】【「默认」】【「嵌套」】【0】，1）；

定 原子命令 为 言令.命令（「原子」，「失败声明不可留下半项」）；

令 默认失败：理 为 假；

试 则
    原子命令.加默认选项（「count」，「n」，「次数」，「数」，「次数」，「坏值」）；
救 所误 则
    置 默认失败 为 真；
    测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_SPEC_DEFAULT」）；
终

测试.断言（默认失败，「非法默认值应失败」）；

原子命令.加默认选项（「count」，「n」，「次数」，「数」，「次数」，1）；

测试.相等（原子命令.解析（【】）【「选项」】【「count」】，1）；

法 非法名称（）：空 则
    言令.命令（「坏	名称」，「」）；
    归 空；
终

法 名称过长（）：空 则
    令 名称：文 为 「甲」；
    当 （长度（名称） 不大于 言令.最大名称字数） 则
        置 名称 为 （名称 加 名称）；
    终
    言令.命令（名称，「」）；
    归 空；
终

法 说明过长（）：空 则
    令 说明：文 为 「甲」；
    当 （长度（说明） 不大于 言令.最大说明字数） 则
        置 说明 为 （说明 加 说明）；
    终
    言令.命令（「说明」，说明）；
    归 空；
终

法 非法短名（）：空 则
    言令.命令（「短名」，「」）.加开关（「verbose」，「-」，「」）；
    归 空；
终

法 重复长名（）：空 则
    定 命令 为 言令.命令（「重复」，「」）；
    命令.加开关（「verbose」，「v」，「」）.加开关（「verbose」，「q」，「」）；
    归 空；
终

法 重复短名（）：空 则
    定 命令 为 言令.命令（「重复」，「」）；
    命令.加开关（「verbose」，「v」，「」）.加开关（「quiet」，「v」，「」）；
    归 空；
终

法 必需顺序错误（）：空 则
    定 命令 为 言令.命令（「顺序」，「」）；
    命令.加参数（「可选」，「」，假）.加参数（「必需」，「」，真）；
    归 空；
终

法 重复余项（）：空 则
    定 命令 为 言令.命令（「余项」，「」）；
    命令.加余项（「第一」，「」）.加余项（「第二」，「」）；
    归 空；
终

法 必需参数后加子命令（）：空 则
    定 根 为 言令.命令（「根」，「」）；
    根.加参数（「入口」，「」，真）.加子命令（言令.命令（「build」，「」））；
    归 空；
终

法 子命令后加必需参数（）：空 则
    定 根 为 言令.命令（「根」，「」）；
    根.加子命令（言令.命令（「build」，「」））.加参数（「入口」，「」，真）；
    归 空；
终

定 环根 为 言令.命令（「环根」，「」）；

定 环子 为 言令.命令（「环子」，「」）；

环根.加子命令（环子）；

法 自环（）：空 则
    环根.加子命令（环根）；
    归 空；
终

法 祖先环（）：空 则
    环子.加子命令（环根）；
    归 空；
终

须报（非法名称，「YANLING_SPEC_NAME」）；

须报（名称过长，「YANLING_SPEC_NAME」）；

须报（说明过长，「YANLING_SPEC_LIMIT」）；

须报（非法短名，「YANLING_SPEC_SHORT」）；

须报（重复长名，「YANLING_SPEC_DUPLICATE」）；

须报（重复短名，「YANLING_SPEC_DUPLICATE」）；

须报（必需顺序错误，「YANLING_SPEC_ORDER」）；

须报（重复余项，「YANLING_SPEC_ORDER」）；

须报（必需参数后加子命令，「YANLING_SPEC_MODE」）；

须报（子命令后加必需参数，「YANLING_SPEC_MODE」）；

须报（自环，「YANLING_SPEC_CYCLE」）；

须报（祖先环，「YANLING_SPEC_CYCLE」）；
