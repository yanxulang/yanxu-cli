引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

令 各实参：列<文> 为 【】；

令 数量：数 为 0；

当 （数量 不大于 言令.最大实参数） 则
    追加（各实参，「甲」）；
    置 数量 为 （数量 加 1）；
终

令 已报：理 为 假；

试 则
    言令.命令（「实参」，「」）.解析（各实参）；
救 所误 则
    置 已报 为 真；
    测试.相等（言令.错误详情（所误）【「代码」】，「YANLING_ARG_LIMIT」）；
终

测试.断言（已报，「第 4097 个实参应被拒绝」）；
