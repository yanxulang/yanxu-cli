引「../src/言令.yx」为 言令；

引「标准:测试」为 测试；

定 根 为 言令.命令（「言包」，「项目与包管理」）；

根.加开关（「verbose」，「v」，「详细输出」）；

定 添加 为 言令.命令（「add」，「添加依赖」）；

添加.加开关（「dev」，「d」，「开发依赖」）.加参数（「包」，「包名」，真）；

根.加子命令（添加）；

定 结果：典 为 根.解析（【「-v」，「add」，「-d」，「http」】）；

测试.相等（结果【「子命令」】，「add」）；

测试.断言（结果【「选项」】【「verbose」】，「根选项应保留」）；

测试.断言（结果【「子结果」】【「选项」】【「dev」】，「子命令选项应解析」）；

测试.相等（结果【「子结果」】【「参数」】【「包」】，「http」）；

定 缺少：典 为 根.尝试解析（【「add」】）；

测试.断言（非 缺少【「成功」】，「子命令必需参数缺失应失败」）；

定 原样 为 言令.命令（「原样」，「测试双连字符」）；

原样.加余项（「值」，「原样参数」）.加子命令（言令.命令（「add」，「不会触发」））；

定 原样结果：典 为 原样.解析（【「--」，「add」】）；

测试.相等（原样结果【「子命令」】，空）；

测试.相等（原样结果【「参数」】【「值」】【0】，「add」）；
