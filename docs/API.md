# 言令 API

`命令（名称，说明）` 提供链式声明：

- `加开关（名称，短名，说明）`
- `加选项（名称，短名，值名，类型，说明，必需）`
- `加默认选项（名称，短名，值名，类型，说明，默认值）`
- `加多值选项（名称，短名，值名，类型，说明）`
- `加参数（名称，说明，必需）`
- `加余项（名称，说明）`
- `加子命令（命令）`

执行接口为 `解析`、`解析当前`、`尝试解析` 与 `帮助`。取值类型支持 `文`、`数`、`理` 和 `JSON`。
