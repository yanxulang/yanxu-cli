引「../src/言令.yx」为 言令；

定 项目 为 言令.命令（「项目」，「管理项目构建」）；

项目.加开关（「verbose」，「v」，「显示详细日志」）.加默认选项（「profile」，「p」，「配置」，「文」，「构建配置」，「dev」）；

定 构建 为 言令.命令（「build」，「构建当前项目」）；

构建.加开关（「release」，「r」，「启用发布优化」）.加多值选项（「feature」，「f」，「能力」，「文」，「启用能力」）；

项目.加子命令（构建）；

定 结果：典 为 项目.解析（【「--verbose」，「build」，「--release」，「-f」，「http」，「-fjson」】）；

言 结果【「命令路径」】；

言 结果【「子结果」】【「选项」】；

定 补全：典 为 项目.补全（【「build」，「--re」】）；

言 补全【「命令路径」】；

言 补全【「候选」】；
