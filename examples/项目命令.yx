引「../src/言令.yx」为 言令；

定 程序 为 言令.命令（「示例」，「一个言序命令行程序」）；

程序.加开关（「verbose」，「v」，「显示详细信息」）.加默认选项（「count」，「n」，「次数」，「数」，「执行次数」，1）.加参数（「名称」，「要问候的人」，真）；

定 解析：典 为 程序.尝试解析（【「-v」，「-n」，「2」，「言序」】）；

若 解析【「成功」】 则
    言 解析【「结果」】；
否则
    言 解析【「错误」】；
    言 解析【「帮助文」】；
终
